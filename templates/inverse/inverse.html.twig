{% extends 'base.html.twig' %}

{% block body %}
<style>
    body {
        /* Ajout de l'image de fond avec fallback couleur */
        background: url('/images/inverse.webp') center center/cover no-repeat fixed, linear-gradient(120deg, #1a1a1a 60%, #3a0000 100%);
        color: #f8f8f8;
        font-family: 'Segoe UI', 'Roboto', Arial, sans-serif;
        min-height: 100vh;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        position: relative;
    }
    p{
        font-family: 'font1';
    }
    /* Overlay sombre allégé pour garder la lisibilité du texte sans masquer l'image */
    .background-overlay {
        position: fixed;
        top: 0; left: 0; right: 0; bottom: 0;
        width: 100vw;
        height: 100vh;
        background: linear-gradient(120deg, #1a1a1a88 60%, #3a000088 100%); /* Opacité réduite */
        z-index: 0;
        pointer-events: none;
    }
    .ctf-container {
        position: relative;
        z-index: 1;
        background: rgba(30, 0, 0, 0.92);
        border-radius: 18px;
        box-shadow: 0 0 32px 8px #ff003c44, 0 2px 8px #000a;
        padding: 2.5rem 2rem 2rem 2rem;
        margin-top: 3rem;
        max-width: 420px;
        width: 100%;
        text-align: center;
        border: 2px solid #ff003c;
        animation: fadeIn 1.2s cubic-bezier(.4,0,.2,1);
    }
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-30px); }
        to { opacity: 1; transform: translateY(0); }
    }
    h1 {
        font-family: 'Bebas Neue', 'Oswald', Impact, sans-serif;
        font-size: 2.5rem;
        letter-spacing: 2px;
        color: #ff003c;
        text-shadow: 0 0 12px #ff003c88, 0 2px 0 #000;
        margin-bottom: 1.2rem;
        animation: flicker 2.5s infinite alternate;
    }
    @keyframes flicker {
        0%, 100% { opacity: 1; text-shadow: 0 0 12px #ff003c88, 0 2px 0 #000; }
        45% { opacity: 0.85; text-shadow: 0 0 18px #ff003c, 0 2px 0 #000; }
        50% { opacity: 0.7; text-shadow: 0 0 24px #ff003c, 0 2px 0 #000; }
        55% { opacity: 0.85; text-shadow: 0 0 18px #ff003c, 0 2px 0 #000; }
    }
    .hint {
        background: #2a0000;
        color: #ffb3c6;
        border-left: 4px solid #ff003c;
        padding: 0.7rem 1rem;
        margin: 1.2rem 0 1.7rem 0;
        border-radius: 8px;
        font-size: 1.05rem;
        box-shadow: 0 0 8px #ff003c33;
    }
    form {
        margin-top: 1.5rem;
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
    input[type="text"] {
        padding: 0.7rem 1rem;
        border-radius: 6px;
        border: 1.5px solid #ff003c;
        background: #1a1a1a;
        color: #fff;
        font-size: 1.1rem;
        outline: none;
        transition: border 0.2s, box-shadow 0.2s;
        box-shadow: 0 0 6px #ff003c22;
    }
    input[type="text"]:focus {
        border: 2px solid #ff003c;
        box-shadow: 0 0 12px #ff003c55;
    }
    button[type="submit"] {
        background: linear-gradient(90deg, #ff003c 60%, #ff6a00 100%);
        color: #fff;
        border: none;
        border-radius: 6px;
        padding: 0.7rem 1.2rem;
        font-size: 1.1rem;
        font-weight: bold;
        letter-spacing: 1px;
        box-shadow: 0 0 12px #ff003c55;
        cursor: pointer;
        transition: background 0.2s, transform 0.15s;
        margin-top: 0.2rem;
        animation: pulse 2s infinite alternate;
    }
    @keyframes pulse {
        0% { box-shadow: 0 0 12px #ff003c55; }
        100% { box-shadow: 0 0 24px #ff003c99; transform: scale(1.03); }
    }
    button[type="submit"]:hover {
        background: linear-gradient(90deg, #ff6a00 60%, #ff003c 100%);
        transform: scale(1.05);
    }
    .error-message {
        color: #ff003c;
        background: #2a0000;
        border-left: 4px solid #ff003c;
        border-radius: 6px;
        padding: 0.5rem 1rem;
        margin-top: 1rem;
        font-weight: bold;
        box-shadow: 0 0 8px #ff003c33;
        animation: shake 0.4s;
    }
    @keyframes shake {
        0% { transform: translateX(0); }
        20% { transform: translateX(-6px); }
        40% { transform: translateX(6px); }
        60% { transform: translateX(-4px); }
        80% { transform: translateX(4px); }
        100% { transform: translateX(0); }
    }
    @media (max-width: 600px) {
        .ctf-container {
            padding: 1.2rem 0.5rem 1.2rem 0.5rem;
            margin-top: 1.2rem;
        }
        h1 {
            font-size: 1.5rem;
        }
    }
</style>
<div class="background-overlay"></div>
<div class="ctf-container">
    <h1>🔒 Bloqué : <br>Monde Parallèle</h1>
    <div class="hint">
        <p>Tu es dans le Monde parallèle !</p>
        <p>Trouves le flag pour en sortir...<p>
        <p>Indice : Regarde du côté de <code>/inverse/data</code></p>
        <p>ps : utilise <span style="color:red"> atob("MonSecretEnBase64");</span></p>
    </div>
    <form action="{{ path('app_submit') }}" method="post">
        <input type="text" name="flag" placeholder="Entre ton flag ici" required>
        <button type="submit">Revenir dans le Monde Normal</button>
    </form>
    {% for message in app.flashes('error') %}
        <div class="error-message">{{ message }}</div>
    {% endfor %}
</div>
{% endblock %}
